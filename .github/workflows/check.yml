name: Dummy test

on:
  pull_request:
  push:
    branches:
      - master

env:
  TEST_DIR: test

jobs:
  check:
    name: Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Dummy test
        run: python ${{env.TEST_DIR}}/dummy_test.py
        shell: sh
